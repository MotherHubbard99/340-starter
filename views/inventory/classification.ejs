<% if (title) { %>
    <h1><%= title %></h1>
    <% } else { %>

    <%#  // redirects the response object to the default index route, to deliver the home view
    //res.redirect('/') //this line was not allowing a sort to happen so commenting it out  %>
    <h1>Inventory</h1>
    <% } %>

<%# messages() %>
<cdiv class = "inventory-grid">
    <%- grid %>
</div>